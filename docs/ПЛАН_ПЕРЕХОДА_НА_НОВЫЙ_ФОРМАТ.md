# План перехода на новый формат выгрузок

> Решение: отказаться от старых файлов (`ВыгрузкаРеал2`, `ВыгрузкаПКО`, …) и
> перейти на новый пакет из папки `INNOWAVE GROUP UPLOAD` — 123 файла,
> четыре базы. Документ написан ДО работ: сначала считаем риски, потом
> трогаем код. Состояние на 23.08.2026.

## 1. Главный риск и что с ним

Новые файлы покрывают **тот же период**, что уже загружен. Если ключ
дедупликации изменится, история задвоится. Проверил на реальных файлах,
сравнив ключи старого и нового формата напрямую.

### Реализации — безопасно

Ключ: `дата · контрагент · номенклатура · количество · цена · сумма · номер ·
склад · сумма документа`. Значения в обоих форматах одинаковы, отличаются
только имена колонок.

```
строк: старый 5299 · новый 5294
совпало ключей: 5289
только в старом: 10 · только в новом: 5
```

Расхождения — не формат, а разное время снимка: в старом файле есть строки
за 20.08.2026, в новом — документ БАЙГО от 23.07.2025 на 5 527 274,4
(тот самый, что расходится с SalesDoc на 13,92).

**Вывод: реализации можно грузить новым форматом поверх старых данных,
дублей не будет.**

### Оплаты — задвоятся, если не поправить

Ключ: `дата со временем · сумма · валюта · контрагент · вид операции`.
Дата хранится **строкой как есть**, а форматы записи времени разные:

| Формат | Пример |
|---|---|
| старый | `11.12.2025 4:36:57` |
| новый | `19.12.2025 09:00:03` |

Час без ведущего нуля против часа с нулём — строки разные, хеши разные.

```
как есть (строкой)     совпало 1012 · только старый 636 · только новый 638
с нормализацией даты   совпало 1647 · только старый   1 · только новый   3
```

**638 оплат приехали бы вторыми экземплярами.** Лечится одной правкой:
разбирать дату в `datetime` и приводить к единому виду перед хешированием.
После неё сходится 1 647 из 1 648.

### Что ещё проверить тем же способом

Закупки, возвраты, списания, расходы — у каждого свой ключ. Проверка
занимает пять минут на таблицу и должна быть сделана **до** первой загрузки,
а не после.

## 2. Что ломается в импортёрах

Проверено запуском настоящих функций на новых файлах.

| Импортёр | Что ждёт | Что в новом формате | Итог сейчас |
|---|---|---|---|
| Продажи | `КонтрагентНаименование`, `НоменклатураНаименование` | `Контрагент`, `Номенклатура` | **0 строк, 20 ошибок** |
| Возвраты | то же | то же | **0 строк, 20 ошибок** |
| Закупки | + `СкладНаименование` | `Склад` | не распознает |
| Деньги (ПКО, РКО, ПП) | `Сумма` | `СуммаДокумента`, `СуммаПлатежа` | «Не найдены колонки поступлений» |
| Списания | понимает оба варианта | — | работает |

Плюс два промаха классификатора:

- `Дополнительные расходы` → уезжает в денежные расходы (это landed cost,
  84 строки, задвоит расходы);
- `Платежный ордер списание ДС` → уезжает в списание товаров (это банк).

И налоговые файлы (72 штуки) не открываются вовсе: 1С ред. 1.7 пишет в архив
`xl/SharedStrings.xml` с большой буквы, openpyxl ищет строчную. Починка в
портале есть, но живёт только в `tax.py`.

## 3. Фирма и контур: одна папка вместо четырёх

Сейчас Apps Script помечает файл фирмой **по папке** и смотрит на три папки,
среди которых `INNOWAVE GROUP UPLOAD` нет — то есть ни один из 123 файлов
никуда не едет.

Но из четырёх решений при загрузке три уже берутся из имени файла: вид
документа, контур (`НАЛОГОВАЯ` в имени переключает на налоговый импортёр) и
формат (по колонкам). Из папки берётся только **фирма**, а она тоже написана
в имени первым словом.

**Решение: определять фирму по имени файла, папку оставить запасным
вариантом.** Тогда одна папка работает, перекладывать ничего не нужно, и
файл, случайно оказавшийся не там, приедет туда, куда назван, — сегодня он
молча уехал бы в чужую фирму.

## 4. Порядок работ

Каждый шаг проверяем до следующего.

1. **Нормализовать дату в ключе оплат.** Без этого нельзя грузить деньги
   вообще. Проверка: пересчитать ключи на старом и новом файле — должно
   сойтись 1 647 из 1 648.
2. **Проверить ключи остальных таблиц** (закупки, возвраты, списания,
   расходы) тем же скриптом. Где ключ поедет — поправить так же.
3. **Расширить карты заголовков**: короткие имена наравне с длинными, суммы
   `СуммаДокумента`/`СуммаПлатежа` как запасные для `Сумма`. Старые файлы
   при этом продолжают грузиться — переход без обрыва.
4. **Развести две коллизии классификатора** (допрасходы, платёжный ордер).
5. **Фирма из имени файла**, папка — запасной вариант.
6. **Вынести починку архива 1.7** из `tax.py` в общий помощник.
7. **Подключить папку** `INNOWAVE GROUP UPLOAD` в Apps Script. Перед этим
   убрать из неё 6 zip-архивов и `EventLog.xml` — скрипт отправляет всё
   подряд.
8. **Загрузить и сверить**: Контроль → Журнал загрузок, затем контрольные
   цифры — выручка за месяц, дебиторка, остатки, число документов. Сравнить
   с тем, что было до перехода: расхождений быть не должно, кроме
   объяснимых.
9. **Отключить старые папки** в Apps Script — только после успешной сверки.

## 5. Чего не хватает в самом пакете

- У Хайджин управленки 23 файла против 28 у Инновейв. Нет: **Корректировка
  долга, ЭСФ выписанные, ЭСФ полученные, Движение МБП, Ручные операции**.
- В списаниях **нет сумм**, только количество — стоимость списаний
  приходится оценивать (по StarKid вышло ≈568 000 сом).
- `СтавкаНСП` в управленке приходит GUID-ом вместо значения.
- `СтавкаНДС`/`СтавкаНСП` в налоговой — с хвостовыми пробелами (`'Станд    '`).
- `Остатки товаров` — сводная таблица с колонкой на каждый склад; при
  добавлении склада имя колонки поменяется. Нужен плоский вид.

## 6. Налоговый контур — отдельным решением

72 налоговых файла сейчас осознанно пропускаются (`tax_skip`), и это
правильно, пока нет отдельного раздела. Смешивать их цифры с
управленческими нельзя: по Инновейв за один и тот же период управленка даёт
20 929 415, налоговая — 26 855 356, и `ДокументGUID` двух баз **не
пересекаются ни одним значением**. Это две независимые картины, а не одна с
уточнениями.

## 7. Откат

Старые файлы никуда не деваются, пока шаг 9 не выполнен. Если после
загрузки цифры разъедутся: вернуть старые папки в Apps Script, а из базы
удалить строки нового импорта по дате загрузки. Ничего необратимого в плане
нет — при условии, что шаги 1 и 2 сделаны и дубли не попали в базу.

## 8. Сплошная проверка: все 123 файла, все колонки

Первая версия плана опиралась на 64 файла Инновейв и пять файлов Хайджин —
налоговый контур Хайджин я не смотрел вовсе. Пробел закрыт: прогнал все 123
файла обеих фирм и обоих контуров.

**565 уникальных названий колонок.** Ни одного старого длинного имени —
`КонтрагентНаименование`, `НоменклатураНаименование`, `СкладНаименование`,
`ВалютаДокументаНаименование`, `ОтветственныйНаименование`,
`ПроцентСкидкиНаценки` — в новом пакете **нет ни разу**. Переход полный:
все старые сопоставления мертвы, запасного варианта «а вдруг придёт старое
имя» держать не для чего (кроме уже загруженных файлов старого образца).

Встречаемость ключевых колонок (из 123 файлов):

| Что | Варианты |
|---|---|
| контрагент | `Контрагент` — 56 · `ПринятоОт` — 6 · `ФизЛицо` — 14 |
| номенклатура | `Номенклатура` — 55 |
| склад | `Склад` — 31 |
| сумма | `СуммаДокумента` — 73 · `Сумма` — 45 · `СуммаПлатежа` — 26 · `Всего` — 12 |
| количество / цена | `Количество` — 49 · `Цена` — 35 |
| дата | `Дата` — 109 · `Период` — 6 (журналы проводок) |
| валюта | `Валюта` — 71 |
| единица | `ЕдИзм` — 40 |
| вид операции | `ВидОперации` — 44 |
| **ДокументGUID** | **115 из 123** |

Восемь файлов без `ДокументGUID` — это справочники (у них `КонтрагентGUID`
и `НоменклатураGUID`) и остатки, где документа нет по определению.

### Проверка достаточности полей

Прогнал классификатор по всем именам и для каждого распознанного вида
проверил, находятся ли обязательные поля хотя бы под одним из новых имён.

```
tax_skip 72 · не распознан 14 · unsupported 13 · expense 6 · receipts 4
writeoffs 4 · cash_balances 2 · stock_balances 2 · purchases 2 · sales 2
return_docs 1 · return_lines 1
```

**Полей не хватает ровно в двух файлах**, и это уже известная коллизия:
`Платежный ордер списание ДС` (обеих фирм) попадает в списание товаров, где
ждут номенклатуру и количество, а это банковский документ. Больше сюрпризов
нет — во всех остальных распознанных файлах обязательные поля находятся.

### 14 «не распознанных» — это виды без импортёра

ГТД по импорту, Журнал проводок (19 743 строки у Хайджин!), Конвертация,
Начисление зарплаты, Проблемные документы, Движение МБП, Ручные операции,
ЭСФ выписанные и полученные. Они уйдут в определение по колонкам и,
скорее всего, лягут в «не распознан» в журнале загрузок. Это не ошибка
перехода — импортёров под них нет и не планировалось на этом шаге. Но
**журнал проводок стоит взять первым же после перехода**: 19 743 проводки
Хайджин и 3 886 Инновейв — это ОСВ, баланс и P&L.

### Что это меняет в плане

Пункт 3 уточняется: карты заголовков не «расширить», а **переписать под
новые имена**, оставив старые только ради уже загруженных файлов. И в
денежных импортёрах нужен не один запасной вариант для суммы, а порядок:
`СуммаДокумента` → `СуммаПлатежа` → `Сумма`.

---

## 9. Налоговый контур переведён (проверено на 72 файлах)

### 9.1. Вид операции теперь берётся только из имени файла

Раньше вид угадывался по заголовкам. На новом пакете это давало прямые
ошибки:

| Файл | Куда попадал | Правильно |
|---|---|---|
| Поступление товары (300 строк Хайджин) | реализации | закупки |
| Поступление услуги (303 строки) | реализации | закупки |
| Поступление доп расходов (192 строки) | реализации, 0 строк | закупки |
| Авансовый отчет оплата (134 строки) | возвраты | авансовые отчёты |
| Списание товаров (108 строк) | реализации, 0 строк | списания |

Итого **1 037 строк Хайджин уехали бы не в тот вид**, из них 795 —
в реализации, прямо в сверку с управленкой. Теперь имя файла разбирается
явной картой на 36 видов, а незнакомое имя отбивается ошибкой: молчаливая
догадка тут дороже отказа.

Виды сверки: реализации, возвраты, деньги приход, деньги расход. Остальные
(закупки, авансовые отчёты, списания, возвраты поставщикам, оприходование,
перемещения, инвентаризация) лежат справочным реестром без колонки
«Управленка» — управленческого контура под них в портале нет.

Не грузятся осознанно, с внятной причиной: журналы проводок, ГТД,
счета-фактуры и ЭСФ, конвертация, начисление зарплаты, ручные операции,
бланки СФ, справочники.

### 9.2. Найдена ошибка выгрузки: «СуммаПлатежа» не привязана к шапке

В денежных файлах (ПКО, РКО, платёжные поручения) колонки расшифровки
платежа приезжают от чужих документов:

```
Хайджин, Приходный кассовый ордер — 49 документов
  сумма по «СуммаДокумента»:  40 467 679,11
  сумма по «СуммаПлатежа»:   104 458 284,00
```

Видно построчно: у документа С0000000001 от 15.10.2024 на **89 сом** в
колонке «СуммаПлатежа» стоит **3 200 000**. Такая же картина в РКО
(25 022 704,53 против 19 997 749,98) и в ПП исходящее (39 713 358,42
против 39 978 320,80). У ПП входящее колонки совпадают до копейки.

Товарных документов это НЕ касается: по всем 175 реализациям Хайджин и 191
Инновейв строки табличной части сходятся с итогом документа до копейки,
расхождений ноль. Значит выгрузка ломается именно на расшифровке платежа.

Пока: деньги берём из шапки документа («СуммаДокумента»), «СуммаПлатежа»
не читаем вовсе. **Эрмеку стоит показать этот пример** — если расшифровку
починят, появится разбивка платежа по договорам.

### 9.3. Классификация больше не зависит от угадывания по колонкам

Все 123 файла пакета распознаются по имени, «не распознанных» ноль. Это
важно не ради красоты: `Движение МБП` с колонками Дата/Сумма/Контрагент
угадывался как денежный расход и подмешал бы движение малоценки к платежам.

```
tax 72 · unsupported 29 · expense 6 · receipts 4 · sales 2 · purchases 2
writeoffs 2 · cash_balances 2 · stock_balances 2 · return_docs 1 · return_lines 1
```

### 9.4. Что загружается из налоговых баз

Прогон всех 72 файлов дважды подряд: 30 файлов с данными, 42 пустых или
неподдерживаемых, 4 281 строка, повторный проход не добавил ни одной —
снапшот заменяется по паре «вид + источник», файлы одного вида друг друга
не затирают.

| Фирма | Вид | Источник | Строк | Сумма |
|---|---|---|---:|---:|
| Хайджин | реализации | Реализация товары | 733 | 138 257 623,10 |
| Хайджин | деньги приход | ПП входящее | 320 | 128 995 025,23 |
| Хайджин | деньги приход | Приходный кассовый ордер | 49 | 40 467 679,11 |
| Хайджин | деньги приход | ПКО выдача в подотчет | 20 | 9 395,11 |
| Хайджин | деньги расход | ПП исходящее | 484 | 39 713 358,42 |
| Хайджин | деньги расход | Расходный кассовый ордер | 83 | 25 022 704,53 |
| Хайджин | деньги расход | РКО выдача в подотчет | 37 | 18 083 954,55 |
| Хайджин | деньги расход | ПП исходящее налоги | 55 | 2 929 459,32 |
| Хайджин | деньги расход | Платежный ордер списание | 172 | 45 541,32 |
| Хайджин | закупки | Поступление доп расходов | 192 | 13 904 531,91 |
| Хайджин | закупки | Поступление услуги | 303 | 8 879 932,38 |
| Хайджин | закупки | Поступление товары | 300 | 4 362 156,50 |
| Хайджин | авансовые отчёты | Авансовый отчет оплата | 134 | 20 096 216,26 |
| Хайджин | возвраты | Возврат от покупателя | 31 | 173 407,50 |
| Хайджин | списания | Списание товаров | 108 | без сумм |
| Инновейв | реализации | Реализация товары | 659 | 26 855 356,00 |
| Инновейв | деньги приход | ПП входящее | 69 | 16 558 988,47 |
| Инновейв | деньги приход | Приходный кассовый ордер | 7 | 3 288 345,80 |
| Инновейв | деньги приход | ПКО выдача в подотчет | 2 | 895,80 |
| Инновейв | деньги расход | ПП исходящее | 139 | 4 642 198,97 |
| Инновейв | деньги расход | Расходный кассовый ордер | 21 | 2 450 349,99 |
| Инновейв | деньги расход | РКО выдача в подотчет | 5 | 2 013 770,79 |
| Инновейв | деньги расход | ПП исходящее налоги | 41 | 801 060,64 |
| Инновейв | деньги расход | Платежный ордер списание | 37 | 31 633,95 |
| Инновейв | закупки | Поступление услуги | 98 | 3 314 705,99 |
| Инновейв | закупки | Поступление товары | 61 | 1 812 386,68 |
| Инновейв | закупки | Поступление доп расходов | 26 | 36 044,60 |
| Инновейв | авансовые отчёты | Авансовый отчет оплата | 27 | 2 011 666,50 |
| Инновейв | возвраты | Возврат от покупателя | 63 | 10 319 341,60 |
| Инновейв | списания | Списание товаров | 5 | без сумм |

### 9.5. Что просить у Эрмека

1. **Расшифровка платежа в ПКО/РКО/ПП** — колонки «СуммаПлатежа» и
   «СуммаВзаиморасчетов» приезжают от чужих документов (пример в 9.2).
2. **Суммы в «Списание товаров»** — «СуммаДокумента» пуста во всех 113
   строках обеих фирм, есть только количество.
3. Пустые файлы, которые должны быть непустыми (по обеим фирмам):
   Платежный ордер поступление, РКО выплата зарплаты, Реализация услуги,
   Возврат поставщику, Оприходование, Перемещение, Инвентаризация,
   Авансовый отчёт (движение ДС / документы выдачи / прочее).

---

## 10. Что вскрыл первый боевой прогон

Первый прогон автоприёма по новой папке прошёл на сборке до фиксов раздела 9
и показал две ошибки задвоения, которых лабораторные проверки не поймали.

### 10.1. Расходы задваивались: номер документа в ключе дедупликации

Старая выгрузка расходов состояла из пяти колонок — `Дата, Сумма, Валюта,
Контрагент, ВидОперации`. Колонки `Номер` в ней **не было вовсе**. Ключ
дедупликации расходов номер включал, поэтому ни одна старая строка не
сходилась с новой:

```
Хайджин РКО (821 строка):      добавлено 611, дублей 210
Хайджин ПП исходящее (534):    добавлено 100, дублей 434
Инновейв ПП исходящее (164):   добавлено 164, дублей   0
```

Поступления той же беды избежали: их ключ номера не содержит, и ПКО дал
1 новую строку из 1650. Это и подсказало причину.

Номер из ключа убран. Проверено на реальных файлах: старый формат → новый
даёт 821 строку вместо 1642.

Плюс уборка уже задвоившегося: при импорте расходов старая безномерная
строка удаляется, если у неё нашёлся номерной двойник с теми же датой,
суммой, валютой, контрагентом и видом. Пары считаются одна к одной — старый
платёж без пары не трогается. На воспроизведении боевого состояния: 2 217
строк схлопнулись ровно в 1 519, столько их и есть в файлах.

### 10.2. Реализации Инновейва задваивались: сменился тип выгрузки

Инновейв раньше выгружался **документами** — одна строка = один документ с
итогом, номенклатура заменялась заглушкой «Реализация (документ)». Новый
пакет даёт ту же реализацию **построчно**, 636 строк. Это разные импортёры
и разные хеши, поэтому 636 построчных строк легли поверх документных:
выручка Инновейва удвоилась.

Теперь построчный импорт удаляет документные строки своей фирмы за тот же
период. Проверено: 188 документов + 636 строк дают 636 строк, документных
не остаётся, повторный прогон ничего не меняет.

### 10.3. Три ошибки из раздела 9 в логе — та же сборка

`ImportError: _load_wb` у закупок и списаний и «Не найдены колонки остатков
денег» — это ровно те поломки, что чинит коммит раздела 9. Apps Script не
помечает файл доставленным при ответе не 200, поэтому после деплоя он
подберёт их сам, без вмешательства.

---

## 11. Второй прогон: управленка сошлась полностью

База на момент прогона была пуста — у каждой таблицы первый же файл дал
`total_in_db`, равный своему `added`. Загрузилась она начисто, и итоги
сошлись с эталоном полного пакета до строки:

| Таблица | В базе | Эталон |
|---|---:|---:|
| Реализации | 5 936 | 5 936 |
| Поступления | 2 132 | 2 132 |
| Расходы | 1 558 | 1 558 |
| Закупки | 353 | 353 |
| Списания | 459 | 459 |
| Остатки товаров | 61 | 61 |
| Остатки денег | 12 | 12 |
| Возвраты | 325 | 325 |

Задвоения нет нигде: `skipped_duplicates` и `legacy_twins_removed` по нулям
именно потому, что дублировать было нечего.

### 11.1. Семнадцать налоговых файлов Хайджина не переотправились

Apps Script помнит доставленные файлы и повторно их не шлёт. В прогоне
участвовали все 36 налоговых файлов Инновейва и только 19 из 36 Хайджина.
Не приехали, в частности, **Реализация товары (733 строки)**, Возврат от
покупателя, Списание товаров и все три Поступления.

Если налоговую таблицу чистили вместе с остальными, налоговый контур
Хайджина сейчас неполон. Лечится функцией `resendAll()` в Apps Script: она
стирает память о доставленных файлах и шлёт весь пакет заново. Повторная
отправка безопасна — все импортёры идемпотентны, проверено двойным
прогоном всех 123 файлов.
